import styled from "styled-components"
import { FlexWrapper } from "../../components/FlexWrapper"

export const Footer = () => {
    return(
        <StyledFooter>
            <FlexWrapper>41112</FlexWrapper>
        </StyledFooter>
    )
}

const StyledFooter = styled.footer`
    
`